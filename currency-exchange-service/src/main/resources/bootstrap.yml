spring:
  application:
    name: currency-exchange-service
  cloud: 
    config: 
      url: http://localhost:8888

eureka:
  client:
    service-url:
      default-zone=http://localhost:8761/eureka:

